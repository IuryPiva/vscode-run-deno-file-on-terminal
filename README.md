# run-deno-file-on-terminal README

Run current file on a new terminal with `deno run -a --unstable`.

## Features

Run current file on a new terminal with `deno run -a --unstable`.

### 1.0.0

Initial release

-----------------------------------------------------------------------------------------------------------
