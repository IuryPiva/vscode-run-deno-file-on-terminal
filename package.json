{
  "name": "run-deno-file-on-terminal",
  "displayName": "deno run ${file}",
  "description": "Run current file on a new terminal with `deno run -a --unstable`",
  "version": "0.0.1",
  "engines": {
    "vscode": "1.63.2"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:run-deno-file-on-terminal.runCurrentDenoFileOnTerminal"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "run-deno-file-on-terminal.runCurrentDenoFileOnTerminal",
        "title": "Run current file on a new terminal with `deno run -a --unstable`."
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "test": "node ./out/test/runTest.js"
  },
  "devDependencies": {
    "@types/vscode": "^1.63.0",
    "@types/glob": "^7.1.4",
    "@types/mocha": "^9.0.0",
    "@types/node": "14.x",
    "glob": "^7.1.7",
    "mocha": "^9.1.3",
    "typescript": "^4.4.4",
    "@vscode/test-electron": "^1.6.2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/IuryPiva/vscode-run-deno-file-on-terminal"
  },
  "publisher": "IuryPiva"
}